name: ceg
description: "Claim-level counterfactual verification using CLIP patches, blur masks, and MNLI CPS."
defaults:
  top_k: 3
  entailment_threshold: 0.7
  risk_mode: targeted_vqa_support_guard
